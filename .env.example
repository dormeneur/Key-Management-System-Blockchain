# ============================================================
# Blockchain KMS — Environment Variables
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ============================================================

# ── Hardhat / Smart Contract Deployment ─────────────────────
# Alchemy (or any Sepolia RPC provider) HTTP URL
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_API_KEY

# Private key of the deployer wallet (WITHOUT the 0x prefix)
# ⚠️  NEVER commit this to Git!
DEPLOYER_PRIVATE_KEY=your_metamask_private_key_here

# Etherscan API key (for contract verification)
ETHERSCAN_API_KEY=your_etherscan_api_key_here

# ── Frontend (Vite env vars must start with VITE_) ──────────
# Pinata JWT for IPFS uploads
VITE_PINATA_JWT=your_pinata_jwt_here

# Deployed contract address (filled after running deploy script)
VITE_CONTRACT_ADDRESS=0x_YOUR_DEPLOYED_CONTRACT_ADDRESS

# Sepolia RPC URL for the frontend (can be same as above)
VITE_SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_API_KEY
