{
  "name": "blockchain-kms",
  "version": "1.0.0",
  "description": "Blockchain-based Key Management System — College Project",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.0",
    "@openzeppelin/contracts": "^5.0.0"
  },
  "keywords": ["blockchain", "kms", "ethereum", "sepolia", "ipfs"],
  "license": "MIT"
}
